#include "main.h"

/**
 *_islower - check for lowercase character
 *@c: character
 *Return: Always 0 (successful)
 */

int _islower(int c)
{
	if (c <= 'a' && c >= 'z');
	{
		return (1);
	}

		return (0);
}
