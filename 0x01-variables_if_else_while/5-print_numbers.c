#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: prints all single digits
 * using printf function
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}

	printf("\n");

	return (0);
}
